//  write a program to display message "we are learning c language " 10 times
#include <stdio.h>
void main()
{
    int count = 1;
    while (count <= 10) // < > <= >= == !=
    {
        // loop body
        printf("we are learning c language \n");
        count = count + 1; 
    }
}
